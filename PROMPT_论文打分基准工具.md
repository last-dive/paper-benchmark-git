# 任务 — 论文打分基准工具：单文件 HTML，自定义模型接口，多论文批量评分与雷达图

> 致执行方：这是一个独立开发任务，交给你是因为需要**真正的设计智能**——工具的灵魂不在功能清单，而在**让 LLM 打分产生统计意义的方法设计**。我给出完整的问题背景、意图与已知的坑；方案、架构、rubric 设计、统计方法由你全权决定。**已知的问题与对策是我的思考起点，不是约束——若你有更好的方法，请推翻它们并说明理由。**
>
> 最终产物：**单个自包含 HTML 文件**（浏览器直接打开即用），对多篇论文进行结构化专业评分，输出排序、雷达图、评分理由，并支持**版本间对比**——用于定量衡量同一论文的迭代改进。

---

## 1. 这个工具为什么存在（问题背景）

**用途**：对多篇论文（PDF/文本）做结构化专业评分与排序，服务三个目的：
1. **定量衡量同一论文的版本迭代**——版本间五维度分差（核心需求，详见 §4）；
2. **盲审模拟**——发现作者自己看不到的弱项；
3. **领域定位**——与已发表论文的基准分对照。

**输入**：论文以 PDF 或纯文本提供；评分由**自定义大模型接口**执行（OpenAI 兼容格式，endpoint/key/模型名由用户配置）。

**本质难点**（这个工具的成败在此）：LLM 打分存在**固有方差与漂移**——同一篇论文两次打分可能差 ±0.5–1 分；打分标准会随批次上下文漂移；排序受"当前批次相对水平"影响。**如果不解决这些，产出的数字只是幻觉般的精确。** 我的初步对策（每篇独立调用、多次取中位、rubric 锚定、排序放本地）写在 §3，请审阅、改进或推翻。

---

## 2. 使用场景与用户（影响 UI 与交互设计）

- **用户**：论文作者本人（非程序员背景，但能看懂统计概念）；
- **典型流程**：上传 2–30 篇论文（同一论文的多个版本 + 对标文献）→ 配置模型接口 → 一键评分 → 看排序与雷达图 → 切换版本对比模式看迭代效果；
- **输出消费方式**：屏幕上看雷达图与分差表；导出 JSON/CSV 存档；把理由摘录贴进修订笔记。

---

## 3. 评分方法论（我的思考起点——请审阅、改进或推翻）

### 3.1 评分维度

我初步设定五个维度（各 1–10 分）：**贡献清晰度、方法严谨性、实验充分性、对比公平性、主张-证据一致性**。

但请注意：**这个维度集是针对"我的论文"（工程方法学论文）定制的**。若你认为评分维度本身应该更通用、或应按论文类型自适应（数值仿真型/实验型/理论型论文的评分侧重不同），请重新设计——维度设计的好坏直接决定打分的区分度。

### 3.2 打分方差问题——我的对策与局限

**已知问题**：
- 同一篇论文同参数两次调用，分数仍可能抖动（数值路径、注意力模式）；
- 打分标准漂移：LLM 对"什么是 7 分"没有稳定锚点；
- 批次位置偏差：30 篇一起评时，分数受批次相对水平影响；
- **量表问题**：LLM 打的"分"没有物理单位，直接排序与差值的统计意义存疑。

**我的初步对策**：
- 每篇独立调用（不批量喂给 LLM）；排序在本地代码算（不让 LLM 排）；
- 每篇打 3 次取中位数；rubric 里写锚定描述（1 分/10 分各代表什么）；
- 版本对比用"同 prompt 同模型同日重打全部版本"，差值才有效；
- 接受 ±0.5 分噪声底——版本间差异小于此视为无显著变化。

**局限与你的空间**：
- 锚定描述可能把输出锚死在 rubric 作者的认知水平——是否有更好的锚定方式（如 pair-wise 比较、reference distribution、校准集）？
- 3 次取中位是否最优？多次调用的方差结构与样本数的关系？
- 是否应该引入**相对比较**作为主方法（A vs B 哪个好）而非绝对打分？相对比较的方差可能更小、区分度更高——但如何与绝对分数共存？
- **若你的设计与我不同，请直接采用你的设计并说明理由**——我只要求最终输出能回答"版本间是否有显著改进、改进在哪个维度"。

### 3.3 评分 prompt 的构造

评分 prompt 需要告诉 LLM：评分维度定义、输出格式（建议结构化 JSON 便于解析）、以及**评分时应采用的视角**（建议：IEEE Transactions 级别审稿人、关注方法学与证据结构）。

我的一个未决问题：**是否应该在评分 prompt 中给 LLM 提供"锚定样例"**（已知好坏的论文片段+分数）？锚定可能提升稳定性，但也会把标准锚死在样例选择上。请你权衡。

---

## 4. 版本对比模式（核心需求）

**场景**：同一论文的多个版本（如 v10→v13→v20→v23，横跨结构瘦身/风格化/硬度提升三个改进阶段），需要定量回答"迭代是否有显著改进、改进在哪个维度"。

**我的初步设计**：
- 选一个基准版本，其他版本相对它显示五维度 Δ 分；
- 雷达图叠加对比；
- Δ<0.5 分视为噪声底。

**请改进的方向**：
- Δ 的显著性判断是否应更精细（如基于 3 次打分的分布做检验，而非固定阈值）？
- **评分理由的对比**可能比分数差更有信息量——同一论文两个版本的评分理由并排展示，让用户肉眼对比"审稿视角的理由变化"（理由变化往往先于分数变化）；
- 分数差的方向解读：结构改进、风格改进、硬度提升应该反映在**不同维度**的分差上——工具是否可以帮助用户建立"改动类型↔维度变化"的映射提示？

---

## 5. 输出与可视化

**需要覆盖**：
- 评分总表（排序）+ 每篇五维度分 + 总分；
- 雷达图（多篇叠加对比是核心场景）；
- 每篇每次打分的完整理由（可展开）；
- **全部原始数据可导出**（JSON/CSV——包括每次调用的分数/理由/token 消耗；这是审计与复算的基础）。

**可视化与交互的其余部分由你设计**——唯一建议：**理由文本的呈现权重不应低于分数**（对作者而言，"为什么这个维度 7 分"比"7 分"本身更有价值）。

---

## 6. 技术环境

- **单文件自包含 HTML**（内嵌 CSS/JS，浏览器 file:// 直接打开即用，无构建步骤、无 CDN 依赖）；
- 模型接口：OpenAI 兼容 chat completions（endpoint/key/模型名/参数用户配置，localStorage 持久化）；
- 论文文本：PDF 上传（pdf.js 内嵌或引导粘贴文本）+ 直接粘贴；
- 环境提示：CORS 若被 API 拒绝，UI 提示解决方案（本地代理或允许跨域 endpoint）；
- 中文 UI（可双语）；并发/超时/重试可配置；
- **建议持久化的数据**：API 配置、批次、每次打分的原始结果——全部可导出 JSON/CSV。

**其余技术选型由你决定。**

---

## 7. 交付后验收方式（效果导向，非清单导向）

交付后我将用以下材料做**效果验收**（而非功能清单核对）：
- 同一论文的 4 个版本文本（横跨三个改进阶段）+ 2–3 篇已发表 T-AES 论文（领域基准锚定）；
- **验收问题**（请确保你的设计能回答）：
  1. 同一版本两次评分的稳定性如何（方差是否在可接受范围）？
  2. 版本间 Δ 的方向是否与实际改进阶段对应（结构瘦身应影响哪些维度、风格化影响哪些）？
  3. 已发表论文的基准分是否显著高于草稿版本（领域定位是否有效）？
  4. 理由文本是否具体可读（而非泛泛套话）？

若验收中发现打分方差过大或区分度不足，请提出你的改进方案（如调整 rubric、增加相对比较、改变聚合方式）并迭代——**打分的统计有效性是验收的核心标准**。

---

*任务由总设计师定义（2026-09-07）。背景与意图已给全；方案、rubric 设计、统计方法、UI/交互、附加功能由你全权决定。若我的初步对策有误或有更优方案，请推翻并说明理由。*
