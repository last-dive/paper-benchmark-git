# 安全说明

当前仓库保存 v2.5.0 快照，尚未公布正式的受支持版本周期、安全响应时限或专用邮箱。

## 报告漏洞

如果目标 GitHub 仓库启用了私密漏洞报告，请通过仓库 Security 页的 **Report a vulnerability** 提交。若未启用，可先提交仅请求私下联系渠道的 Issue；不要在公开 Issue 附上可利用细节、有效凭据、论文或完整报告。维护者应在正式对外维护前补充明确的私密联系渠道。

私密报告应包含受影响版本、运行方式（源码或 AppImage）、操作系统、最小复现、预期影响和必要的脱敏日志。使用虚构论文与无效占位 Key 复现；不要提供他人的资料或服务凭据。

## 当前运行边界

本机服务监听回环地址，并检查 Host、Origin 与会话凭证。该程序面向单机使用；当前没有针对公网或多用户服务器部署的设计和验证。

默认 Key 存在 `.private/model_config.json`，配置工具设置目录权限 700、文件权限 600。自定义 API Key 用于当前服务会话，默认 Key 不会自动转给自定义接口。不要把真实 Key 放入命令行、示例文件、Git 远程地址或问题报告。

程序会清理已知 Key，但导出报告包含论文内容、输入路径、请求、响应和附件。分享前应检查整份报告目录。`.gitignore` 和仓库扫描不能保证所有私密内容都已排除。

如果凭据已经进入共享渠道，应先在其服务商处撤销或轮换，再处理副本和 Git 历史；删除当前文件不能使已传播的凭据恢复安全。

MinerU 转换调用固定的主机脚本并访问主机 GPU、模型和输出目录。AppImage 不提供这些主机依赖，也不把该工作流变成隔离的多用户服务。具体要求见 [README](README.md) 和 [AppImage 构建说明](docs/APPIMAGE_BUILD.md)。
